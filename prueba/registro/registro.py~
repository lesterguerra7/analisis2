from registro.models import registro


def registrar(nuevo): #verifica si el nuevo registro ya existe por nickname
	try:
		us = registro.objects.get(nick=nuevo)
		if us:
			return True
		else:
			return False
	except ValueError:
		return False
